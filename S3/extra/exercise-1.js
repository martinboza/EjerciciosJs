
    // Obtén el elemento con la clase .fn-remove-me
    const elementToRemove = document.querySelector('.fn-remove-me');

    // Verifica si el elemento existe antes de intentar eliminarlo
    if (elementToRemove) {
        // Elimina el elemento
        elementToRemove.remove();
    }